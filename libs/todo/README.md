# todo-infra

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build todo-infra` to build the library.

## Running unit tests

Run `nx test todo-infra` to execute the unit tests via [Vitest](https://vitest.dev/).
