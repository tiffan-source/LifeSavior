# domain-protocol

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domain-protocol` to build the library.

## Running unit tests

Run `nx test domain-protocol` to execute the unit tests via [Vitest](https://vitest.dev/).
