# business-logic

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build business-logic` to build the library.

## Running unit tests

Run `nx test business-logic` to execute the unit tests via [Vitest](https://vitest.dev/).
